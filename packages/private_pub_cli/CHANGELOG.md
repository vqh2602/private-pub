## 0.1.0

- Initial release with private registry `check`, `versions`, and `compare`.
- Add Dart/Flutter `pub outdated` and `pub upgrade` command delegation.
- Add support for major-version, dry-run, prerelease, and CI workflows.
