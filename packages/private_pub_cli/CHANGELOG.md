## 0.1.1

- Bump version to coordinate with the registry release.

## 0.1.0

- Add OAuth PKCE `login`, credential storage, and Dart SDK `setup`.
- Add smart `publish` without source pubspec edits.
- Add topological monorepo `prepare` and `publish --auto`.
- Add authenticated MCP stdio tools for package search and source reading.

## 0.0.3

- Initial release with private registry `check`, `versions`, and `compare`.
- Add Dart/Flutter `pub outdated` and `pub upgrade` command delegation.
- Add support for major-version, dry-run, prerelease, and CI workflows.
