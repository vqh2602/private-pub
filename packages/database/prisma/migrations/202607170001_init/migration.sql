CREATE EXTENSION IF NOT EXISTS pg_trgm;
-- The remaining tables are generated from schema.prisma with `pnpm db:migrate`.
-- Keeping the extension in version control makes fuzzy package-name search portable.
